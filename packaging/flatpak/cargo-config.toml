# Cargo configuration written into .cargo/config.toml at build time so
# the offline Flatpak build can resolve dependencies from the vendored
# sources generated by flatpak-cargo-generator.py.

[source.crates-io]
replace-with = "vendored-sources"

[source.vendored-sources]
directory = "/run/build/wflow/cargo/vendor"
